[project]
name = "graphene-blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.4",
    "graphene-django>=3.2.3",
    "python-decouple>=3.8",
]
